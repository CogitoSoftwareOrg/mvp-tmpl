name: mvp-tmpl

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ENV=${ENV:-production}
      - PUBLIC_POSTHOG_TOKEN=${PUBLIC_POSTHOG_TOKEN:-phc_mZiQVCTX8I9xuUk0KAdpXzwMmE4aMGMFsfqcEwaci1Y}
      - PUBLIC_POSTHOG_URL=${PUBLIC_POSTHOG_URL:-https://eu.i.posthog.com}
      - PUBLIC_PB_URL=${PUBLIC_PB_URL:-https://mvp-pb.cogitosoftware.nl}
      - MEILI_URL=${MEILI_URL:-https://mvp-meili.cogitosoftware.nl}
      - LANGFUSE_PUBLIC_KEY=${LANGFUSE_PUBLIC_KEY:-pk-lf-f7e217ed-2076-40cf-8a8c-d3da536fff2c}
      - LANGFUSE_BASE_URL=${LANGFUSE_BASE_URL:-https://langfuse.quizbee.academy}
      - CRAWL4AI_USER=${CRAWL4AI_USER:-admin}
      - CRAWL4AI_URL=${CRAWL4AI_URL:-https://crawl4ai.quizbee.academy}
      - UNSTRUCTURED_URL=${UNSTRUCTURED_URL:-https://unstructured.cogitosoftware.nl}
      - LITELLM_URL=${LITELLM_URL:-https://litellm.hackseeker.com}

      # SECRETS
      - LITELLM_API_KEY=${LITELLM_API_KEY}
      - CRAWL4AI_API_KEY=${CRAWL4AI_API_KEY}
      - UNSTRUCTURED_API_KEY=${UNSTRUCTURED_API_KEY}
      - PB_EMAIL=${PB_EMAIL}
      - PB_PASSWORD=${PB_PASSWORD}
      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY}
      - LANGFUSE_SECRET_KEY=${LANGFUSE_SECRET_KEY}
      - STRIPE_API_KEY=${STRIPE_API_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - TG_ID=${TG_ID}
      - TG_TOKEN=${TG_TOKEN}

      - FIRECRAWL_API_KEY=${FIRECRAWL_API_KEY}
